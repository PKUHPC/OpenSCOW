# 集群显示名称
displayName: hpc01Name

# 各个登录节点的IP或者域名，服务节点必须可访问
loginNodes:
  - login01
  - login02

# 适配器地址(ip地址:端口号)
adapterUrl: localhost:8972

# 门户系统代理网关节点配置
# proxyGateway:
#   # 带协议，不要以/结尾
#   url: http://10.2.3.2:1080

#   # 是否在这个机器上自动配置nginx
#   autoSetupNginx: true
